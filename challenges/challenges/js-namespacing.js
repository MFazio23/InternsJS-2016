/**
 * Created by MFazio on 6/7/2016.
 */

var skyline = {
    location: 'Wisconsin',
    getName: function() {
        return "Skyline Technologies";
    },
    getFullName: function(first, last) {
        return 'Dr. ' + first + ' ' + last;
    }
};

getName = function() {
    return 'Browser';
};

getFullName = function(first, last) {
    return first + ' ' + last;
};